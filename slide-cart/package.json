{
  "name": "slide-cart",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && aws s3 sync ./dist/build s3://zecpe-public/upsell"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.19",
    "vite": "^5.4.8"
  },
  "dependencies": {
    "@rollup/plugin-replace": "^6.0.1",
    "@sentry/vite-plugin": "^2.22.5"
  }
}
